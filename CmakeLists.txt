cmake_minimum_required(VERSION 2.8)
project(RealsenseCmakeTest)

set(CMAKE_EXPORT_COMPILE_COMMANDS 1)

include(CheckCXXCompilerFlag)
CHECK_CXX_COMPILER_FLAG("-std=c++11" COMPILER_SUPPORTS_CXX11)
CHECK_CXX_COMPILER_FLAG("-std=c++0x" COMPILER_SUPPORTS_CXX0X)
if(COMPILER_SUPPORTS_CXX11)
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -std=c11")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
elseif(COMPILER_SUPPORTS_CXX0X)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++0x")
endif()

set(realsense2_DIR "C:/Program Files (x86)/librealsense2/lib/cmake/realsense2")
message(${realsense2_DIR})
find_package(realsense2 REQUIRED )

add_executable( test_pcl rs-getting-started.cpp )
include_directories(include ${realsense_INCLUDE_DIR})
target_include_directories(test_pcl PRIVATE ${realsense_INCLUDE_DIR})
target_link_libraries(test_pcl ${realsense2_LIBRARY})
